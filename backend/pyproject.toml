[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "abstractly"
version = "0.1.0"
description = "A research paper discovery backend powered by Semantic Scholar."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["abstractly"]